{{!< default}}

<style>
/* Force scroll on account page - inline to ensure priority */
html, body {
    overflow: visible !important;
    overflow-y: scroll !important;
    height: auto !important;
    max-height: none !important;
}

.site-main {
    overflow: visible !important;
    height: auto !important;
    padding-top: 100px !important;
}

.content-wrapper {
    margin-top: 0 !important;
    padding-top: 0 !important;
}
</style>

<div class="content-wrapper account-page">
    {{#if @member}}
    <div class="account-container">
        
        {{!-- Header Section --}}
        <header class="account-header">
            <div class="account-header-icon">👤</div>
            <h1 class="account-title">
                <span class="account-prompt">&gt;_</span> Mon Compte
            </h1>
            <p class="account-subtitle">Gérer vos informations et préférences</p>
        </header>

        {{!-- Member Info Card --}}
        <section class="account-section">
            <div class="account-section-header">
                <h2 class="account-section-title">
                    <span class="section-icon">ℹ️</span>
                    Informations
                </h2>
            </div>
            <div class="account-card">
                <div class="account-info-grid">
                    <div class="account-info-item">
                        <span class="account-info-label">Nom</span>
                        <span class="account-info-value">{{@member.name}}</span>
                    </div>
                    <div class="account-info-item">
                        <span class="account-info-label">Email</span>
                        <span class="account-info-value">{{@member.email}}</span>
                    </div>
                    <div class="account-info-item">
                        <span class="account-info-label">Statut</span>
                        <span class="account-info-value account-status {{#if @member.paid}}paid{{else}}free{{/if}}">
                            {{#if @member.paid}}Premium{{else}}Gratuit{{/if}}
                        </span>
                    </div>
                    <div class="account-info-item">
                        <span class="account-info-label">Membre depuis</span>
                        <span class="account-info-value">{{date @member.created_at format="D MMMM YYYY"}}</span>
                    </div>
                </div>
                <div class="account-actions">
                    <a href="#/portal/account/profile" class="account-btn account-btn--ghost" data-portal="edit-profile">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/>
                            <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/>
                        </svg>
                        Modifier le profil
                    </a>
                </div>
            </div>
        </section>

        {{!-- Subscription Section (if paid member) --}}
        {{#if @member.paid}}
        <section class="account-section">
            <div class="account-section-header">
                <h2 class="account-section-title">
                    <span class="section-icon">💳</span>
                    Abonnement
                </h2>
            </div>
            <div class="account-card">
                <div class="account-subscription-info">
                    <div class="subscription-badge">
                        <span class="badge-icon">⚡</span>
                        <span class="badge-text">Membre Premium</span>
                    </div>
                    <p class="subscription-description">
                        Vous avez accès à tous les contenus exclusifs et aux fonctionnalités premium du blog.
                    </p>
                </div>
                <div class="account-actions">
                    <a href="#/portal/account/plans" class="account-btn account-btn--ghost" data-portal="manage-subscription">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <circle cx="12" cy="12" r="3"/>
                            <path d="M12 1v6m0 6v6m5.196-13.196l-4.242 4.242M7.046 7.046L2.804 2.804m18.392 0l-4.242 4.242m4.242 13.15l-4.242-4.242M2.804 21.196l4.242-4.242"/>
                        </svg>
                        Gérer l'abonnement
                    </a>
                </div>
            </div>
        </section>
        {{/if}}

        {{!-- Newsletter Section --}}
        <section class="account-section">
            <div class="account-section-header">
                <h2 class="account-section-title">
                    <span class="section-icon">📬</span>
                    Préférences
                </h2>
            </div>
            <div class="account-card">
                <div class="account-preferences">
                    <div class="preference-item">
                        <div class="preference-info">
                            <span class="preference-label">Newsletter</span>
                            <span class="preference-desc">Recevoir les derniers articles par email</span>
                        </div>
                        <div class="preference-toggle">
                            {{#if @member.newsletters}}
                                <span class="toggle-status active">✓ Activée</span>
                            {{else}}
                                <span class="toggle-status inactive">○ Désactivée</span>
                            {{/if}}
                        </div>
                    </div>
                </div>
                <div class="account-actions">
                    <a href="#/portal/account/newsletters" class="account-btn account-btn--ghost" data-portal="manage-newsletters">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                            <polyline points="22,6 12,13 2,6"/>
                        </svg>
                        Gérer les newsletters
                    </a>
                </div>
            </div>
        </section>

        {{!-- Danger Zone --}}
        <section class="account-section account-section--danger">
            <div class="account-section-header">
                <h2 class="account-section-title">
                    <span class="section-icon">⚠️</span>
                    Zone de danger
                </h2>
            </div>
            <div class="account-card account-card--danger">
                <p class="danger-description">
                    Déconnexion ou suppression de votre compte.
                </p>
                <div class="account-actions account-actions--danger">
                    <a href="/auth/member/logout" class="account-btn account-btn--danger">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
                            <polyline points="16 17 21 12 16 7"/>
                            <line x1="21" y1="12" x2="9" y2="12"/>
                        </svg>
                        Se déconnecter
                    </a>
                </div>
            </div>
        </section>

    </div>

    {{else}}
    {{!-- Not logged in state --}}
    <div class="account-container account-logged-out">
        <div class="logged-out-content">
            <div class="logged-out-icon">🔒</div>
            <h1 class="logged-out-title">Accès restreint</h1>
            <p class="logged-out-message">Vous devez être connecté pour accéder à cette page.</p>
            <div class="logged-out-actions">
                <a href="/auth/member/login?action=signin" class="account-btn account-btn--primary">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4"/>
                        <polyline points="10 17 15 12 10 7"/>
                        <line x1="15" y1="12" x2="3" y2="12"/>
                    </svg>
                    Se connecter
                </a>
                <a href="{{@site.url}}" class="account-btn account-btn--ghost">
                    Retour à l'accueil
                </a>
            </div>
        </div>
    </div>
    {{/if}}
</div>
